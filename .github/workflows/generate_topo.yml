on:
  push:
    paths:
    - "inet-henge/**"

defaults:
  run:
    working-directory: ./netomox

jobs:
  generate-topo:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: prepare playground 
      env:
        GITHUB_IO_REPO_DEPLOY_KEY: ${{ secrets.REPO_DEPLOY_KEY }}
      run: |
        echo "$GITHUB_IO_REPO_DEPLOY_KEY" > ~/deploy_key.pem
        chmod 600 ~/deploy_key.pem
        git clone -b feature/gha git@github.com:ool-mddo/playground.git

    - name: delete file
      run: rm -f ./playground/pushed-flug/*

    - name: make file
      run: touch ./playground/pushed-flug/202202demo

    - name: write date
      run: echo `date` > ./playground/pushed-flug/202202demo

    - name: commit files
      env:
        GIT_SSH_COMMAND: ssh -i ~/deploy_key.pem -o StrictHostKeyChecking=no -F /dev/null
      run: |
          cd ./playground
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git commit -m "Update from pushed-config"
          git push
